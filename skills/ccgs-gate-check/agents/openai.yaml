interface:
  display_name: "CCGS Gate Check"
  short_description: "Claude Code Game Studios workflow: gate-check"
  default_prompt: "Use $ccgs-gate-check to apply the migrated gate-check workflow from Claude Code Game Studios in this workspace."

policy:
  allow_implicit_invocation: false
